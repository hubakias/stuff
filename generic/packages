# My desktop/laptop
solaar pandoc(+ tex dependencies)

# Generic
shellcheck bash zsh nmap zmap ldnsutils

# Nice/handy tools
pv termsaver bsdmainutils ssh-agent-filter


# When isolated in a Windows domain, consider the below
# cntlm
